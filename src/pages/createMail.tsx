import type { FC } from 'react';

import { MainLayout } from '@/layouts/MainLayout';

const CreateMail: FC = () => {
  return (
    <MainLayout>
      <p>Create Mail</p>
    </MainLayout>
  );
};

export default CreateMail;
